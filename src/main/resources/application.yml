spring:
    application:
        name: t1_hw4
    datasource:
        url: jdbc:postgresql://localhost:5433/
        username: postgres
        password: "0000"
        driverClassName: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: validate
